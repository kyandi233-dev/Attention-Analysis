# 053｜RGB 分析路线与开发边界

**Status: Proposed**

## 决策背景

NIR 正式分析已经完成并形成稳定 AMD/DirectML runtime；RGB 在 `rgb-dev` 上进行正式视频方法验证，不直接改动稳定 NIR 路线。RGB 目标是建立可与 NIR/Behavior 对齐的连续行为测量，而不是直接生成 Attention Score。

## 当前路线

1. **Motion**：OpenCV frame-difference global Motion 已在 `sub-031` 正式视频上完成 gap/QC/review 验证；
2. **Pose**：MediaPipe Tasks Pose Landmarker / Pose Lite / VIDEO mode，`sub-031` 10 fps 完整正式时间窗 pilot 已完成；
3. **Face**：下一开发步骤直接进入 Py-Feat vs LibreFace 2.0 benchmark。

## 信息保留决策

RGB 强制遵循 `docs/040-rgb/044-RGB输出Schema与信息保留原则.md`：昂贵推理尽量只运行一次，raw 层优先保存以后难以恢复的完整模型输出；QC 先 flag 后 filter；derived feature 与 summary 尽量从 raw 重建；正式结果通过 manifest 记录模型、配置、版本、hash、Git commit 和源文件 provenance。

## Pose 10 fps 决策

`sub-031` 首次完整正式时间窗 Pose pilot：15,494 个采样时点，`pose_valid_fraction=1.0`，0 个 multi-pose frame，约 429.1 s（7.15 min），实际约 36.1 inference/s，输出 511,302 landmark rows / 约 22.7 MB。

因此撤销此前基于速度担忧提出的临时 5 fps 方案，正式开发参数恢复 **10 fps**。10 fps 在当前时间预算内可接受，并保留更高时间分辨率。非采样帧继续只 `grab` 推进 AVI，采样帧才 decode。

## 上半身机位决策

正式 RGB 机位主要看到坐姿被试上半身，因此不能用全部 33 landmark 的 mean visibility/presence 判断 Pose 质量。肩、肘、腕和 nose 是主要 QC；hip 是 optional trunk landmark；膝、踝、脚长期出画时不作为正式质量否决依据。

新增 `pose-qc` 只读取已保存 Parquet，逐 landmark 报告 frame coverage、visibility/presence 分布、阈值比例、in-frame 与 world-coordinate availability；不重新跑 MediaPipe。

## Pose-derived motion 与 body Motion Energy 分界

必须区分：

- **Pose-derived upper-body motion**：由肩/肘/腕 landmark 的位置变化直接计算，按真实 dt 和 shoulder width 标准化；可完全从 `pose-test.parquet` 重建；
- **body_motion_energy**：在被试人体 ROI 内计算像素帧差，需要重新消费 RGB 帧。

现阶段新增 `pose-features` 从已有 Pose raw 生成 wrist/elbow/shoulder/upper-body motion、shoulder center/width 和可用时的 trunk angle。`body_motion_energy` 不在此阶段单独再扫一次 AVI；等待 Face backend 冻结后，与 Motion/Pose/Face 的正式统一视频读取一起计算。

## Face benchmark 顺序

Face benchmark 与 `pose-qc` / `pose-features` 工程上独立。为了赶时间，不等待 Pose 派生指标完全冻结：本地立即跑两个 Parquet 后处理 stage，下一开发步骤同时进入 Py-Feat vs LibreFace benchmark。最终再合成单次视频读取的正式 batch。

## AMD / MediaPipe 边界

MediaPipe Python Pose 当前使用 CPU/XNNPACK；实测 10 fps Pose 时间可接受，因此不为了 Pose 单独增加 DirectML 转换工程。Face 深度模型仍允许在 benchmark 后尝试 ONNX Runtime DirectML，但 preprocessing/postprocessing 与数值一致性必须验证。

## 当前完成状态

- RGB 文件/时间轴 audit：完成；
- timestamp gap QC：完成；
- global Motion pilot/QC/manual review：完成；
- MediaPipe Pose 10 fps representative pilot：完成；
- upper-body Pose QC：代码已实现，待读取现有 Parquet；
- Pose-derived upper-body features：代码已实现，待读取现有 Parquet；
- body Motion Energy：留到统一视频读取；
- Py-Feat vs LibreFace benchmark：下一开发步骤。

## 何时改为 Accepted

至少完成：upper-body Pose QC、Pose-derived feature sanity check、Face benchmark、raw schema 完整性检查、Face backend 与正式采样/QC 规则冻结；若 Face 采用 DirectML，再完成参考 backend 数值一致性验证。
