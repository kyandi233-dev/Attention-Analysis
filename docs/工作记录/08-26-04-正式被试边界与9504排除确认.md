# 08-26-04｜正式被试边界与 9504 排除确认

## 总结

根据用户明确要求，`E:\正式实验` 的正式 cohort 边界固定为被试编号 `>=31`，并显式排除 `sub-9504`。核验发现 NIR 正式 runtime 配置已经满足该边界；当前正式 Behavior 配置原先只有 `min_subject_number: 31`、没有排除项，已补充 `sub-9504` 排除规则。

## 原计划

核验 `E:\正式实验` 是否实际存在 `sub-9504_`，检查 NIR 与 Behavior 当前正式配置的 cohort 边界，并只修正当前正式 Behavior 配置。历史 BBB、旧 formal 配置、训练数据脚本和候选比较配置不属于本次修改范围。

## 执行与决策过程

### 1. 数据目录核验

`E:\正式实验` 实际存在：

```text
E:\正式实验\sub-9504_
```

因此不能仅依赖 `min_subject_number: 31`，必须额外声明排除项。按用户要求，正式候选为 `sub-031` 及以上，但 `sub-9504` 永远排除。

### 2. 当前配置核验

`runtime/nir-formal/config.yaml` 已有：

```yaml
formal:
  min_subject_number: 31
batch:
  subjects:
    exclude:
      - "sub-9504"
```

`configs/behavior_formal.yaml` 原为 `min_subject_number: 31`、`exclude: []`。已改为：

```yaml
min_subject_number: 31
include: []
exclude:
  - "sub-9504"
```

历史 BBB 配置和旧训练/候选脚本中的 `sub-9504` 只保留其历史语义，没有追溯修改。

### 3. 发现校验

使用当前正式 Behavior 配置调用 `discover_subjects` 做只读发现校验，结果为：

```text
n_subjects=44
min_number=31
contains_sub_9504=False
```

发现的 44 人集合与当前 NIR full-class 44 人 cohort 一致。

## 最终决策结果

- 当前正式 NIR 与 Behavior 均采用 `subject_number >= 31` 且排除 `sub-9504`。
- `sub-9504` 即使存在于数据目录、文件名匹配或被 CLI 显式发现，也不得进入当前正式 cohort。
- 当前正式 Behavior 配置已完成边界修正；不执行统计分析、不改变分析计划、不修改外部数据。

## 已完成事项

- [x] 核验 `E:\正式实验\sub-9504_` 实际存在。
- [x] 核验 NIR runtime 已配置 `min_subject_number: 31` 与 `sub-9504` 排除。
- [x] 将同一排除规则补入 `configs/behavior_formal.yaml`。
- [x] 只读执行 Behavior subject discovery，得到 44 人且不含 `sub-9504`。

## 未完成事项

- [ ] 按用户提供的正式分析计划执行后续 cohort preflight。
- [ ] 将 `sub-9504` 排除状态同步纳入后续联结、协变量和机器学习样本审计输出。
