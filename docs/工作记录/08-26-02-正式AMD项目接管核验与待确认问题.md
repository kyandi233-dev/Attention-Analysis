# 08-26-02｜正式 AMD 项目接管核验与待确认问题

## 总结

本次按用户指定的正式项目根目录完成只读接管核验，并将 `amd-DirectML` 快进同步到远端最新提交 `7607e07`。本次没有修改算法、配置、外部结果或问卷资料，没有删除、覆盖或纳入工作区已有未跟踪资产。

## 原计划

本轮目标是先核验当前分支、工作区、项目入口文档、44 人资料说明、最新工作记录以及外部结果目录，再识别正式进入 cohort preflight 前仍需用户确认的问题。本轮不执行最终统计检验，不自行冻结 QC 阈值、subject 排除、左右眼汇总、primary window、混合模型或机器学习参数。

## 执行与决策过程

### 1. Git 与工作区

- 当前分支为 `amd-DirectML`，同步前相对 `origin/amd-DirectML` 落后 1 个提交；执行 `git pull --ff-only` 后与远端一致，HEAD 为 `7607e07`。
- 工作区仍有多项未跟踪的比较配置、候选模型和历史模型资产。它们属于已有用户资产，本次未读取其内容以外的必要状态，也未删除、覆盖、暂存或提交。
- 远端快进带入了 027 资料说明、08-26-01 工作记录以及相关入口更新。

### 2. 入口文档与历史语境

已按要求读取 `AGENTS.md`、根 `README.md`、`docs/README.md`、`docs/010-overview/README.md`、`docs/020-nir/README.md`、`runtime/nir-formal/README.md`、`runtime/nir-formal/INSTALL.md`、`docs/050-decisions/README.md`、027 资料说明和最新相关工作记录。

当前有效科研口径以稳定 `amd-DirectML` 的 640×400 RITnet 结果为准。027 与 08-26-01 中保留了当时在 `amd-DirectML-ritnet512` checkout 执行资料登记的历史语境，但正文已明确真实 44 人 full-class 结果是 640×400；本次不追溯改写历史记录。

### 3. 外部 NIR 与 Behavior 元数据

- `D:\_AttentionData\Beijing-NIR\amd-directml` 实际发现 44 个 full-class completion marker，唯一 subject 集合与 027 记录一致，且均为 `status=complete`、`ritnet-fullclass-v1.2-fast-qc`。
- marker 元数据一致为 RITnet 输入 `[640, 400]`、分析尺寸 `[320, 160]`、`dml:0`、FP32、batch=16；没有把这批结果误识别为 512×320。
- `sub-035_formal_v3.1.3_yolo_b16_fp32` 旧目录只有 2 个文件、没有 full-class CSV；`sub-035_formal_v3.1.3_yolo-b8_ritnet-b16_fp32` 有完整 fast-QC CSV 和 marker。后续仍按 completion marker 选择，不合并两个目录。
- `E:\正式实验` 当前存在；按文件名核验，44 个 NIR subject 均有 `Block1_B_beh.csv` 与 `Block2_B_beh.csv`。这只是文件存在性核验，不代表行为内容、时间戳或最终联结已经验收。

## 最终决策结果

1. 本轮后续维护基线确定为本机 `Attention-Analysis-amd-DirectML` 的 `amd-DirectML`，真实外部结果基线确定为 `D:\_AttentionData\Beijing-NIR\amd-directml` 的 44 人 640×400 full-class 结果。
2. `v2`、`ritnet512`、比较配置和候选模型继续作为历史/候选资料处理，不进入当前正式分析主线。
3. 下一阶段可以直接开展资料清单、schema 按列名对齐、subject×eye×block 描述性 QC 和 B1/B2 文件联结准备；在正式分析计划到达前，不执行推断性统计和模型选择。

## 待确认事项

- 需要用户提供或指定最终的正式分析计划版本；仓库现有 022/024 等设计文档不自动替代用户后续提供的正式计划。
- 44 个 NIR `sub-XXX` 与问卷 `experiment_id` 的编号交集不能解决问卷重复参与和 `identity_conflict_flag`。需要指定权威身份联结依据，或先输出 unresolved linkage 表供用户逐项确认。
- 需要确认 B1/B2 行为文件与 NIR 的正式联结是否仅使用 subject 编号和 block 文件，还是还需实验记录、时间戳或其他 provenance 条件。
- 后续需由分析计划冻结 QC 阈值、subject×eye×block 的排除规则、左右眼保留方式、primary window、标准化方法和统计模型。

## 已完成事项

- [x] 核验 Git 状态、分支、远端同步和最近提交。
- [x] 读取用户指定的项目入口、runtime、决策、027 资料说明和最新相关工作记录。
- [x] 只读核验 44 个 completion marker、640×400 元数据和 `sub-035` 并存目录。
- [x] 只读核验 44 个 NIR subject 的 B1/B2 行为文件存在性。
- [x] 保留并避开工作区已有未跟踪用户资产。

## 未完成事项

- [ ] 建立正式 cohort preflight 输出。
- [ ] 按列名对齐 106/107 列 full-class CSV 并生成 schema 报告。
- [ ] 完成 subject×eye×block 的描述性完整性与异常标记。
- [ ] 完成 NIR、Behavior B1/B2 与问卷之间的身份联结准备。
- [ ] 等待用户提供正式分析计划后再冻结 QC 和统计模型。
