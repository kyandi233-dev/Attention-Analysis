# 030｜旧入口：NIR cohort 当前本地执行任务

> 此顶层文件已停止作为当前执行入口，仅为兼容旧链接而保留。

当前 ChatGPT → Codex Luna 的唯一任务文件已迁移到：

```text
docs/020-nir/chatgpt-control/030-NIR-cohort当前本地执行任务.md
```

Codex Luna 不应再从本文件读取任务正文，也不要在本文件写入任何执行结果。
