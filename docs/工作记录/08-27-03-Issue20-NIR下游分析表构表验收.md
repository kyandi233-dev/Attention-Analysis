# Issue #20｜NIR 下游分析表构表验收工作记录

> 08-27（Asia/Shanghai）｜状态：已完成本轮工程验收；批量构表按用户要求中止，未完成 44 人 cohort 收口

## 背景

GitHub Issue #20 要求在 `analysis/multimodal-integration` 最新代码上验收正式 NIR `11_analysis_tables` 构表层。当前任务只涉及 `10_analysis_ready` 到 `11_analysis_tables` 的下游构表，不运行显著性模型，不重跑 YOLO/RITnet，不修改 frozen production、既有 analysis-ready 或既有 runtime 文件。

## 目标

1. 核验远端最新代码、四份正式数据契约/运行手册和当前工作区状态；
2. 在 `D:\CondaEnvs\attention-behavior` 中运行 Issue 指定测试；
3. 先运行 `sub-031` representative tables，检查 completion、文件类别、行数与 block track；
4. representative 通过后运行 44 人 `11_analysis_tables` 构表并做契约验收；
5. 回报结果到 GitHub Issue #20，并保留完整的本地 provenance。

## 风险与边界

- Issue 指定的历史绝对路径 `D:\aaawork\07-竞赛\厚璨杯\021-analysisplan\Attention-Analysis-amd-DirectML` 当前不存在；实际执行 checkout 为本地 `analysis/multimodal-integration` worktree。
- 当前 44 人 cohort 是 exploratory/development cohort，不代表后续约 116 人最终 cohort。
- 不在本 Issue 选择 coverage threshold、窗口或统计模型，不运行 `20_formal_statistics`。
- 若发现代码契约错误，只记录并停止；除非明确是可修复且不改变冻结科学规则的代码错误，不自行修改实现。

## 执行计划

1. 从远端同步 `analysis/multimodal-integration` 并确认 HEAD；
2. 运行测试；
3. 运行 sub-031 representative；
4. 检查输出与输入保护；
5. 运行 44 人 tables；
6. 汇总验收证据、写入本记录并回复 Issue #20。

## 校验清单

- tests 结果；
- subjects discovered/completed/failed；
- 每名 subject completion；
- cohort manifest；
- trial/probe/time-on-task 行数；
- missing block track；
- primary binocular window-level `pir_valid_fraction` 摘要；
- coverage 与 temporal gap 摘要；
- source-mode fractions 合法性；
- behavior 对齐、重复 key、异常窗口行数；
- `10_analysis_ready` 与 production 未改变；
- 最终 Git 状态。

## 已执行结果与中止原因

- Issue 指定的 `D:\aaawork\07-竞赛\厚璨杯\021-analysisplan\Attention-Analysis-amd-DirectML` 在本机不存在；实际使用本地 `analysis/multimodal-integration` worktree，已快进同步到 `cec5dd5`。
- Issue 指定的 `D:\CondaEnvs\attention-behavior` 不存在；使用现有 `D:\CondaEnvs\nir-amd`，补齐项目依赖后，三组指定测试全部通过。
- `sub-031` representative 构表通过：status complete、missing block track 为空，9 类输出均生成；trial 864 行、trial window 51,840 行、probe window 480 行、time-on-task 6,912 行。
- 44 人批量构表已启动，但用户在运行中提出需要先澄清这不是最终被试平均/统计分析。运行随后中止；停止前外部输出目录已有 8 名被试 completion 文件，不能视为 44 人完成。
- 本次构表没有做图表、显著性检验、混合模型或被试间平均。它只是把连续的 analysis-ready frame-level PIR，按 Behavior 的 trial/probe/time-on-task 分析单位预先物化成可复用表；实际被试层汇总和模型属于后续统计阶段，不在本 Issue 中执行。
- 已生成的部分 `11_analysis_tables` 输出保留在仓库外，没有删除；未修改 `10_analysis_ready`、production CSV、runtime 或仓库代码。

## 中止后的继续执行

用户确认继续执行 Issue #20。恢复前核验：工作区仅有本工作记录未提交，HEAD 与远端均为 `cec5dd5`，没有残留 `nir_formal_pipeline.py` 进程，外部 `11_analysis_tables` 已有 8 名被试 completion。恢复运行应复用 identity 一致的已完成结果，并继续剩余 subject。

## 当前状态

Issue #20 的 44 人批量验收未完成，未在 Issue 回报“44 人完成”。本轮停止时已有 11/44 名被试 completion：`sub-031` 至 `sub-041`；这 11 名被试的 required artifacts 均存在，且 `missing_block_tracks` 均为空。批量过程中断在最终 cohort manifest 写出前，因此当前 `cohort_manifest.json` 仍只记录 representative `sub-031`，不能作为 44 人 manifest。

本轮没有画图、没有做被试平均、没有显著性检验、没有混合模型、没有进入 `20_formal_statistics`，也没有修改 `10_analysis_ready`、production CSV、runtime 或仓库代码。用户说明当前 NIR 数据仅用于观察管线完善，因此保留部分 `11_analysis_tables` 作为工程运行产物，不把它们解释为正式科学结果。

Issue #20 已回复本轮真实状态：测试通过、representative 通过、11/44 已完成、批量按用户要求中止，以及未完成 44 人 cohort-level 验收的原因。

## Issue #20 最终补充：下游分析管线 validation v2

### 背景与目标

Issue #20 的最新补充任务改为验收 `12_pipeline_validation` 下游工程层，不再继续 `11_analysis_tables` 的剩余 33 名被试构表；不重跑 YOLO/RITnet，不修改原始 NIR、Behavior、既有 `10_analysis_ready`/`11_analysis_tables` 或 runtime，不进入 `20_formal_statistics`。本轮只验证当前已存在的 11 人 analysis tables 能否被完整的下游接口读取、物化表格、生成图形并记录外部扩展不可用状态。

### 执行计划

1. 快进同步 `analysis/multimodal-integration` 到远端最新 HEAD；
2. 在 `D:\CondaEnvs\nir-amd` 中运行 Issue 指定的四组测试；
3. 修复仅影响 validation 图形兼容性的 Matplotlib 3.9+ `labels` 参数迁移，以及 probe raw code 的整数解析兼容性；
4. 运行 `scripts/nir_pipeline_validation.py`；
5. 核验 subject 数、表格行数、图形数量/非空、模型 smoke 状态、extension readiness 和 Git/输入边界。

### 实际执行与决策

- 远端同步后 HEAD 为 `58b7e44`，与 `origin/analysis/multimodal-integration` 一致；当前 checkout 是 `C:\Users\goven\.codex\worktrees\6c85\Attention-Analysis-multimodal-integration`。Issue 中提到的 AMD 正式目录在本机不存在，因此没有把它误报为当前 checkout。
- 首次测试发现 `Axes.boxplot(labels=...)` 与 pandas probe 数值类型兼容问题；修复范围仅限 `src/attention_pipeline/nir_pipeline_validation/plots.py`、`probe_plots.py`、`extended_plots.py`、`probe_analysis.py`，未改变分析字段、窗口、QC 阈值或统计模型。
- 修复后 Issue 指定命令结果为 **20 passed**；存在 1 个 pandas `FutureWarning`（`stack(dropna=False)`），无测试失败。
- validation runner 退出码为 **0**，但由于当前只允许消费已有 11 人表格，`validation_summary.json` 的 `n_subjects=11`，subject 为 `sub-031`–`sub-041`。因此本轮不是 44 人全量下游验收。
- 输出根目录为 `D:\_AttentionData\Beijing-NIR\analysis\nir-behavior-v2\cohort-44-exploratory\12_pipeline_validation`；所要求的 15 个核心 CSV 均存在。关键行数为：`behavior_advanced_subject_block=22`、`trial_dynamic_features_long=48,600`、`trial_multiscale_trajectory=567`、`nogo_precursor_trajectory=6,336`、`probe_dynamic_features_long=52,800`、`probe_rt=220`、`probe_behavior_multiscale=880`、`track_correlations=36`、`track_agreement=6`、`source_mode_qc=1,320`、`coverage_multidimensional=9,240`、`trial_visual_covariates=9,504`、`raw_between_person_pir=11`、`subject_level_summary=11`、`model_smoke_results=89`。
- 共生成 **27 个非空 PNG + 27 个非空 PDF**，包括 fig07–fig16 全部扩展图。代码生成的图形均带 `PIPELINE VALIDATION ONLY — CURRENT NIR VALUES KNOWN INVALID` 标记；这些图不能作为当前 NIR 科学结果解释。
- validation summary 标记 `validation_only=true`、`nir_values_known_invalid=true`，没有设置 exclusion threshold，也没有根据 smoke model 的结果做任何科学判断。模型 smoke 有 15 个 complete、3 个因 `LinAlgError: Singular matrix` failed；这些失败已记录，不把 runner 的退出码 0 误解为所有模型都收敛。
- `extension_readiness.json`：visual properties 为 available（27 rows）；questionnaire 为 unavailable（未配置路径）；OAR 为 `blocked_by_analysis_ready_schema`；RGB 为 `future_interface_only`；raw between-person PIR 为 available（11 人）。
- 检查到的 validation 日志主要为 statsmodels singular covariance / boundary / Hessian 警告，以及上述 3 个 probe smoke model 的奇异矩阵失败。这与当前已知无效 NIR 值和小样本/稀疏设计的 validation-only 状态一致；未据此选择排除规则。
- 没有继续剩余 33 名被试，没有重跑 YOLO/RITnet，没有写入 `20_formal_statistics`。输入层和既有 analysis tables 仅作为读取源；本轮新写入限定在 `12_pipeline_validation` 外部输出及仓库内兼容性修复/本工作记录。

### 最终结果与待确认事项

- 测试套件：通过（20 passed）。
- validation runner：完成（exit code 0），但覆盖 11 人而非 44 人；不得作为 44 人完整结果报告。
- 工程产物：表格、fig07–fig16、extension readiness 和 validation summary 均生成；模型 smoke 的 3 个失败保留为工程状态。
- 科学解释：禁止。当前 NIR 值仍按 known invalid 处理，正式筛选、协变量、QC 阈值、统计模型和正式图表等待修正后的 NIR 与用户批准的分析计划。
- 代码修复和本记录已提交到 `analysis/multimodal-integration`，commit 为 `ec453fe`；外部 validation 输出未纳入 Git。
