# 单模态优先与特征资格分阶段实施

日期：2026-09-12。用户明确授权代码修改、先建立 GitHub Issue 并分阶段实施。

角色：代码实现。代码权威 Attention-Analysis；方法权威 Formal 的 codex/code-fix-ledger / 1.16.4–1.16.10。独立工作树分支 codex/single-modality-first-20260912，基线 5c7c82c53fd06477b8eef3b3ffedb7c630ead1a5（PR #62）。已核验 origin、分支及用户既有修改，并安全 fetch；原 checkout 保留原样。

## 原计划与阶段交付

1. #66：修正过期 Behavior 候选角色，建立 Q1-blind 科学特征资格审核、有限候选和报告入口。完成后可运行单模态测量/分布审核；正式预测还需真实证据冻结。
2. #67：科学模态与设备分离，按已冻结模态独立运行单特征/模态模型、Task B 物化和报告；不要求四模态齐备。
3. #65：在前两阶段基础上补 Behavior 条件增量、Full/删一模态及现实设备组合；保持共同样本、参与者隔离和既有训练核心。

## 风险与验证

- 不能用全队列 Q1/Q2 显著性、预测分数、覆盖率或相关阈值自动选变量。
- 科学资格、有限表示选择和折内技术可估计性分开；未冻结状态不能自动转为正式。
- 旧 NIR 接口与 1.16 测量审核并存，不能把旧字段重新当成最终主表示。
- 用合成回归、文件入口及合法真实输入审核验证；不以测试通过宣布测量效度或正式性能。
- 真实审核产物写仓库外；仅代码、配置、测试和运行说明进入 Git。

## 过程

已建立 #66、#67；#65 为现有第三阶段任务。发现 Behavior YAML 与特征审核仍将 CV、commission/d-prime 写成未冻结，先按最新方法合同纠正。系统 Python 的 pandas=3 超出仓库要求，建立独立测试环境并安装 pandas 2.3.3，不修改系统环境。

## 当前状态

阶段 1 的代码与文件级审核已完成；阶段 2/3 继续待实现，不宣布正式全样本预测已放行。

## 阶段 1 交付与验证

- 更新 Behavior 候选 YAML 和 interface 审核角色：五维主 B、CV/commission 已预声明，mean/median 待 G0；d-prime、SD/MAD/IQR 保留测量表但不默认进入主 B。
- 新增单模态 catalog、feature_qualification 模块和 single_modality_feature_review.py 文件入口。科学 modality 与设备依赖分别登记；pupil/心肺未冻结宽表字段明确待定。
- 显式决策绑定源文件和目录 SHA256，只有主候选、证据、实际设备/预处理依赖均符合合同才形成 qualified representation；仍不授权拟合。
- 复用 Behavior 原有 RT-CV/omission 校验；有限覆盖、坏字符串、Inf 和真正缺失分轨，不删行、不插补、不按标签/覆盖率自动筛选。
- 测试命令：`python -m pytest tests/test_single_modality_feature_review.py tests/test_behavior_task_c_supervised_interface.py tests/test_supervised_learning_feature_registry.py tests/test_supervised_learning_entrypoint.py tests/test_supervised_vertical_integration.py -o addopts=''`。最终结果 54 passed in 7.05s。测试环境 Python 3.14、pandas 2.3.3；没有改动系统 pandas。
- 真实审核：对外部 formal_v3/probe_primary_30s.csv 执行 Behavior review，2320 probe、116 session、61 participant group；没有重新生产行为、改写源表或运行预测。源 SHA256 为 `677524c8dfb92bde245d53e9af6b6f06e97624e5739fcce153eda4988a39229a`。最新输出目录为数据区 `feature-review-20260912-stage1-behavior-v3`，三个聚合/状态文件均已生成。
- 表示差异限定为 catalog 显式声明的同单位比较，不将 CV 与 SD、眨眼频率与持续时间直接相减；初次新回归因负向测试仍保留旧 comparison-pair 设置而提前触发其他 schema 拒绝，已修正测试隔离目标。
- 真实输入发现：全部 2320 行仍使用 rt_cv_min_n=20，504 个至少有 2 次正确 Go RT 的窗口仍被旧门槛置为 CV 缺失。新审核明确报告 producer contract failed，不能把这份旧表直接交给当前 Task A。应按当前 Behavior producer/配置重新生成隔离版本，而非回填旧 CSV。

## 可开展与尚未开展

现在可运行单模态变量分布、覆盖、有限表示差异与质量合同审核，编写测量变量及质量问题说明。尚未做真实 Q1 推断、模型拟合、NIR 参数冻结、心肺资格判定或多模态比较。#67/#65 与真实 Behavior 重跑继续是后续工作。
