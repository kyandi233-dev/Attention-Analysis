# 08-26-03｜重复被试与行为联结约束确认

## 总结

本次根据用户补充要求，核验问卷派生数据中的重复被试与身份冲突标记，并明确后续正式工作边界：重复参与和身份冲突必须进入协变量构造、训练样本选择及数据联结审计；NIR 与 Behavior 的联结必须结合实验记录、行为文件时间戳、NIR phase/block 窗口和其他 provenance；正式分析计划由用户后续提供，项目执行不自行优化分析方案。

## 原计划

本次只核验用户指定的两份本地派生 CSV，确认重复和身份冲突字段实际存在并记录计数；不读取或复制手机号、开放题原文，不修改问卷派生文件，不进行受试者合并或排除。

## 执行与决策过程

读取：

```text
D:\AAAWORK\07-竞赛\厚璨杯\021-analysisplan\SART\问卷\问卷分析\questionnaire_derived_data.csv
D:\AAAWORK\07-竞赛\厚璨杯\021-analysisplan\SART\问卷\问卷分析\subject_repeat_registry.csv
```

两份文件均为 146 行，并实际包含：

- `is_repeat_participant`：0 为 81 条，1 为 65 条；
- `identity_conflict_flag`：0 为 117 条，1 为 29 条；
- `experiment_id`：存在 3 条空值，另有 1 条非三位格式的 `1236`。

`questionnaire_derived_data.csv` 还包含 `visit_order`、`prior_visit_count`、`total_visit_count`、`is_first_visit`、`is_cross_stage_repeat`、`is_within_stage_repeat`、`repeat_group`、`identity_match_basis`、`identity_conflict_fields` 等联结审计字段。上述异常仅登记为 preflight 标记，不据此自动修正、合并或排除记录。

用户进一步明确：

1. 重复被试和身份冲突必须在正式分析中显式考虑，包括协变量构造和机器学习训练样本选择；
2. NIR—Behavior 联结不能只使用 subject 编号或文件名，必须核验实验记录、行为文件时间戳、NIR phase/block 窗口和其他 provenance 条件；
3. 用户后续提供正式分析计划，项目侧负责本地文件说明、执行、核验和产物整理，不负责自行优化分析方法或替代计划做决定。

## 最终决策结果

- 重复/身份冲突字段列入后续 cohort preflight 的必检字段和输出报告，不做静默折叠。
- 任何协变量编码、训练样本选择、subject 排除和重复参与处理均等待正式分析计划；执行时保留原始标记和派生决策依据。
- NIR、Behavior B1/B2、实验记录和问卷的联结需要形成 provenance-aware linkage 表，至少记录来源文件、时间戳/窗口匹配、block 对应、联结依据、冲突状态和人工待确认状态。
- 空 `experiment_id` 与非标准编号保留为异常，不自动填补或改写。

## 已完成事项

- [x] 核验 `questionnaire_derived_data.csv` 和 `subject_repeat_registry.csv` 实际存在。
- [x] 核验两份文件均为 146 行。
- [x] 核验重复参与和身份冲突字段及计数。
- [x] 发现并登记空 `experiment_id` 与非三位编号异常。
- [x] 明确后续执行服从用户提供的正式分析计划。

## 未完成事项

- [ ] 等待用户提供正式分析计划。
- [ ] 按计划建立 provenance-aware NIR—Behavior—问卷联结表。
- [ ] 按列名对齐 44 份 full-class CSV。
- [ ] 生成 subject×eye×block 描述性完整性、异常和重复参与标记报告。
- [ ] 按正式计划执行协变量构造与训练样本选择审计。
