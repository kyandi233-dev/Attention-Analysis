# 00｜RGB 目录与映射

## 当前状态（2026-08-23）

RGB 作为独立研究模态继续保留，但当前没有重新开启正式 rPPG / HR / HRV 分析，也没有因为 NIR 全量分析已经完成就自动进入跨模态融合。

当前仓库整理原则是：**保留模态入口，不把未完成或未重新验证的 RGB 结论混入已经完成的 NIR 主线。**

目前本目录没有新的正式 RGB 方法正文或可运行生产入口。若后续重新启动 RGB，应单独核验：

- 正式 RGB 视频完整性与被试覆盖；
- rPPG 算法和运行环境；
- HR / HRV 指标定义与质量门；
- 与行为/NIR 时间轴的同步；
- 缺失/截断数据的处理规则。

历史上 `sub-003` 的 RGB 文件曾被记录为功能性缺失/截断；该判断属于早期数据审计记录，具体证据继续保留在配置与工作记录中。

---

## 历史说明

> 2026-08-13 15:35（Asia/Shanghai）｜RGB 接口保留但关闭，不迁移未验证的 rPPG 结论；当时不包含 rPPG 修复、HR/HRV 正式输出。

这条历史边界继续有效于“未重新验证前不启用 RGB”，但不再用 08-13 的项目阶段描述 2026-08-23 的 NIR 状态。
