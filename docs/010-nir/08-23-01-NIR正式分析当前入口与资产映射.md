# NIR 正式分析当前入口与资产映射

> 2026-08-23（Asia/Shanghai）｜本文件记录当前有效入口。正式 NIR 全量分析已经执行；08-21/08-22 文档继续作为开发和环境配置历史保留。

## 当前入口

```text
branch: main
runtime: runtime/nir-formal/
config: runtime/nir-formal/config.yaml
```

正式模式：

```text
FocusWave v3.1.3 phase windows
        ↓
逐帧 YOLO26n 眼框
        ↓
标准化 ROI
        ↓
RITnet batch inference
        ↓
frames.csv / eyes.csv / summary.json / run_manifest.json / phase_windows.json / overlays
```

正式配置当前为：

- phases：`baseline / instructions / practice / block1 / block2`
- baseline：180 s
- YOLO：逐帧运行，`tracking.method=none`
- RITnet：`batch_size=16`
- precision：`fp32`
- 正式被试范围：`sub-031+`

## 正式模型资产

YOLO26n 训练源：

```text
yolotrain/runs/yolo26n_eye_100epoch/weights/best.pt
```

runtime 自包含副本：

```text
runtime/nir-formal/models/nir-eye-yolo26n-best.pt
runtime/nir-formal/models/ritnet-best_model.pkl
```

NIR 眼框数据版本：

```text
datasets/nir-eye-dataset-v1/
```

## 当前运行命令

```cmd
git switch main
git pull
cd /d <repo>\runtime\nir-formal
conda activate D:\conda_envs\eye-ai
python run_pipeline.py check-env
python run_pipeline.py discover --formal-only
python run_formal_batch.py --dry-run
python run_formal_batch.py
```

单被试：

```cmd
python run_pipeline.py formal --subject sub-033 --root "F:\正式实验"
```

## 与旧文档的关系

`08-22-04-NIR新电脑GPU环境配置与正式批处理运行指南.md` 记录 RTX 4060 机器从零配置和排错过程，继续保留。阅读其中命令时，当前结构对应关系为：

```text
codex/nir-formal-gpu-v3
→ main

runtime/nir-yolo-tracking-ritnet-v1/
→ runtime/nir-formal/
```

历史工作记录中的旧分支名、旧 runtime 名和当时的 production-candidate 状态不修改；它们用于研究与开发过程溯源，不作为当前状态入口。
