# 045｜AMD RGB 开发环境与运行指令

> `amd-DirectML` 是 AMD 综合线；当前 AMD RGB 的 active development branch 是 `rgb-amd`。两者属于同一个 Attention-Analysis 项目，拆分只是为了并行开发、减少冲突。分支关系见 [`../010-overview/015-并行分支与同步约定.md`](../010-overview/015-并行分支与同步约定.md)。

## 1. 当前推荐工作目录

AMD RGB 新开发优先使用：

```text
D:\aaawork\07-竞赛\厚璨杯\021-analysisplan\Attention-Analysis-rgb-amd
```

branch：

```text
rgb-amd
```

每次开始：

```powershell
cd "D:\aaawork\07-竞赛\厚璨杯\021-analysisplan\Attention-Analysis-rgb-amd"
git status --short --branch
git branch --show-current
git fetch origin --prune
git pull --ff-only
```

`amd-DirectML` 继续作为 AMD 综合线保留；已经成熟的 NIR / Behavior / RGB 资产可以同步到 `rgb-amd`，而 `rgb-amd` 通过验收的新 RGB 改动再回并综合线。

## 2. RGB 环境矩阵

| 环境 | Python | 当前用途 |
|---|---:|---|
| `D:\CondaEnvs\attention-rgb` | 3.11 | RGB audit、gaps、Motion、Pose、Face frame preparation、QC |
| `D:\CondaEnvs\attention-face-directml` | 3.11 | AMD Py-Feat ONNX / DirectML Face 正式推理与 derived |
| `D:\CondaEnvs\attention-face-pyfeat` | 3.11 | Py-Feat 官方 reference / ONNX export；非正式 AMD runtime |
| `D:\CondaEnvs\attention-face-libreface` | 3.9 | LibreFace 历史 reference / export |

Git branch 与 Conda 环境彼此独立。

## 3. 主 RGB 环境

```powershell
conda activate "D:\CondaEnvs\attention-rgb"
cd "D:\aaawork\07-竞赛\厚璨杯\021-analysisplan\Attention-Analysis-rgb-amd"
```

基础检查：

```powershell
python -c "import cv2,numpy,pandas,pyarrow,mediapipe; print('rgb core env ok')"
python scripts/rgb_analysis.py --stage audit
python scripts/rgb_analysis.py --stage gaps
```

若环境丢失才重建：

```powershell
conda create -p "D:\CondaEnvs\attention-rgb" python=3.11 -y
conda activate "D:\CondaEnvs\attention-rgb"
python -m pip install --upgrade pip
python -m pip install -e .
python -m pip install pyarrow pytest mediapipe
```

## 4. Face DirectML 环境

```powershell
conda activate "D:\CondaEnvs\attention-face-directml"
python -c "import onnxruntime as ort; print(ort.__version__); print(ort.get_available_providers())"
```

必须包含：

```text
DmlExecutionProvider
```

不要同时安装普通 `onnxruntime` 或 `onnxruntime-gpu`。

当前正式 Face 定义：

```text
Py-Feat 2.1.1 scientific core
ONNX Runtime DirectML
15 Hz timestamp-driven
RetinaFace B8
multitask B16
identity excluded
original AVI direct decode
```

## 5. 正式单被试全程入口

当前已经实现：

```text
scripts/face_formal_prepare.py
scripts/rgb_formal_motion_pose.py
scripts/face_formal_directml.py
scripts/face_formal_derive.py
scripts/run_rgb_formal_subject.ps1
```

推荐通过总控运行：

```powershell
cd "D:\aaawork\07-竞赛\厚璨杯\021-analysisplan\Attention-Analysis-rgb-amd"

$env:ATTENTION_FACE_MODEL_DIR = "D:\_AttentionData\Beijing-RGB\_test\face-directml\models\pyfeat"

powershell -ExecutionPolicy Bypass -File .\scripts\run_rgb_formal_subject.ps1 `
  -Subject sub-031
```

总控会自动调用两个已有 Conda 环境，顺序为：

```text
1. 完整正式时间段 15 Hz Face frame manifest
2. Motion full-fps + Pose 10 Hz + Pose derived
3. original AVI → Face DirectML full-span raw
4. continuous primary tracking + eyelid derived
```

正式输出：

```text
D:\_AttentionData\Beijing-RGB\sub-031
```

当前下一步就是让 `sub-031` 首次完整跑通。通过后再补 44 人 batch + resume。

## 6. 开发/QC 单独入口

Motion：

```powershell
python scripts/rgb_analysis.py --stage motion --subject sub-031
python scripts/rgb_analysis.py --stage motion-qc --subject sub-031
python scripts/rgb_analysis.py --stage motion-review --subject sub-031
```

Pose：

```powershell
python scripts/rgb_analysis.py --stage pose --subject sub-031
python scripts/rgb_analysis.py --stage pose-qc --subject sub-031
python scripts/rgb_analysis.py --stage pose-features --subject sub-031
```

这些开发入口可能继续写 `_test`；正式 subject 输出优先使用总控脚本。

## 7. 历史/representative 验证入口

继续保留：

```text
face_formal_dryrun_sample.py
face_formal_dryrun_directml_v02.py
face_derive_tracking_eyelid_v02.py
face_qc_visualize_v03.py
face_real_directml_pyfeat.py
face_real_parity_v03.py
```

它们用于 provenance、A/B、parity 和 QC，不再是正式全视频入口。

## 8. 当前仍未完成

- 44 人 batch runner；
- completion / resume cohort orchestration；
- `body_motion_energy`；
- blink event threshold；
- `perclos80_proxy` 最终窗口、分母与 QC；
- cohort-level QC 汇总。

这些事项需要完成后才能称为 RGB 科学分析完全冻结，但不再阻挡首个单被试全程自动化验收。

## 9. 输出位置

```text
D:\_AttentionData\Beijing-RGB
```

测试/benchmark：

```text
D:\_AttentionData\Beijing-RGB\_test
```

正式被试：

```text
D:\_AttentionData\Beijing-RGB\sub-XXX
```
