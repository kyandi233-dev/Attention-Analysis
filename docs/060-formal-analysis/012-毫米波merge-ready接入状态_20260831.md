# 毫米波 merge-ready 接入状态（2026-08-31）

> 状态：mmWave producer 侧已产出 probe merge-ready 表；本仓库仍只消费、不复制 producer 逻辑（`external_producer_then_future_formal_ingest`）。

## 1. 已产出的资产

| 文件 | 覆盖 | 位置 |
|---|---|---|
| `mmwave_probe_merge_ready.csv` | J 盘 72 sessions / 1440 probe 窗口 | `D:\Project\厚粲杯\11_数据\_FormalAnalysis\mmWave\` |
| `mmwave_probe_merge_ready_E.csv` | E 盘 44 sessions / 880 probe 窗口 | 同上 |
| 两表合计 | **116 场 / 2320 窗口**（与 Behavior formal_v3 的 116 场一致） | |

producer 侧入口（权威仓库 `greenboo26/focuswave-multimodal-attention-analysis`）：

- `scripts/maintenance/run_mmwave_probe_merge_ready_20260831.py`（J 盘 72 sessions，时间线 `canonical_probe_timeline.csv`）
- `scripts/maintenance/run_mmwave_probe_merge_ready_E_20260831.py`（E 盘 44 sessions，时间线 `probe_primary_30s.csv`）

## 2. 表结构与消费方式

列结构 = canonical probe timeline 33 列（与其他模态 merge-ready 表逐列一致）+ 22 个 `mmwave_*` 字段。五键与其他三模态相同：`repeat_participant_id, session_id, block_id, probe_id, window_name`。

按五键与其他模态表横向拼接即可得到多模态宽表；`repeat_participant_id` 当前为 R 格式（mother table 编码），与 Behavior/NIR/RGB 的 merge-ready 表一致（R→`participant_group_id` 的统一迁移是身份键审计 007 的待办，四模态一起做）。

## 3. 字段科学分层（按 `MMWAVE_FEATURE_CONTRACT_V1.csv`）

| 层 | 字段 | 消费规则 |
|---|---|---|
| JOIN_KEY | 五键 + `window_name` | 仅定位行 |
| STRUCTURAL_ALLOW | `mmwave_state/observed/missing_reason/loadable/timestamp_coverage_fraction` + 质量 2 项 | 可作 audit/覆盖/敏感性，不作生理标签 |
| SUPPORTING_HOLD | HR freq/time/fused + BR（各 `_median`） | **NOT_PRIMARY**：可作 supporting 特征，不可作主要预测变量；HR/BR 仍 `HOLD / SUPPORTING_ONLY` |
| DIAGNOSTIC_HOLD | bin/channel/distance proxy/switch/phase/motion | 可进入未来正式特征块，需先冻结语义 |
| EXCLUDE | IBI/RMSSD/SDNN | 恒 null，禁止消费 |

HR/BR 的 supporting 数字来源：08-31 重跑（协议 pre_30s 窗口 + 完整 selector 链），HR 25s fused MAE 9.02 bpm、BR MAE 2.44 breaths/min（3 targeted subject / 60 窗，详见 producer 仓库 `docs/results/2026-08-31_MMWAVE_PRE30S_SELECTOR_HR/`）。这不是晋升 HR/BR 出 HOLD 的证据，只是 merge-ready 表里 supporting 值的来源说明。

## 4. 缺失语义

- E 盘 5 个无效占位（sub-036/038/040/041 空 timestamps、sub-047 空目录）记 `STRUCTURAL_MISSING`；
- 缺失不填 0、不伪装 success、不从 cohort 删除 session；
- mmWave 缺失只影响 mmWave availability，不改变 Behavior/NIR/RGB 的 session 或 participant identity。

## 5. 尚未做的事（本仓库侧）

- `formal_multimodal_v2.yaml` 的 `path_contract.required_keys` 尚未声明 `mmwave_output_root`（`paths.local.yaml` 已有该键）；补 key 后启动校验才会覆盖 mmWave 输出路径；
- 多模态融合仍 `disabled_deferred`；单模态全部冻结后才恢复 merge/fusion；
- 116 场统一身份键（R→`participant_group_id`）为四模态共同待办。
