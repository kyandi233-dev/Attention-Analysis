# AGENTS.md｜Attention-Analysis 仓库工作规则

> 2026-08-23｜正式 NIR 全量分析已经完成。当前仓库工作重点是整理、归档、复现与维护；不得把项目状态回退为“准备正式分析”“等待 ROI 准入”或“尚未运行全量”。

## 必读入口

开始修改前至少读取：

1. `README.md`
2. `000-项目总览与架构.md`
3. `docs/00-目录与映射.md`
4. 与当前任务直接相关的模态/方法/架构/决策入口
5. 需要追溯历史原因时再读取 `docs/工作记录/00-目录与映射.md` 及对应工作记录

不要把历史工作记录中的旧阶段描述覆盖当前入口文档中的最新状态。

## 当前事实基线

- 一百多名正式被试的数据采集已经完成。
- NIR 双眼 ROI 数据集 v1 已完成并已经用于 YOLO26n 训练。
- YOLO26n 100 epochs 训练和冻结 test 评价已经完成；`best.pt` 是已经存在并使用过的研究资产，不是未来待训练文件。
- 正式 NIR 主链为 `YOLO 眼框检测 → tracking/ROI 更新 → RITnet 分割 → 时序 QC/指标`，并且已经对正式数据完成全量运行。
- 当前任务是仓库结构和复现资产整理，不是重新进行生产准入。

## 历史配置与当前状态

- `configs/formal.yaml` 是早期 ROI 入围/候选阶段的历史兼容配置，内部 candidate/blocked 状态不得用于判断当前项目是否已完成正式分析。
- MediaPipe、YuNet、YOLO-face、face-parts、PuRe/PuReST、DeepVOG、pye3d 等历史候选和比较方案可以继续保留，用于研究 provenance 和复现。
- 历史 Gate、benchmark、ROI selection、smoke test 等 artifacts 可以保留；不要因为当前正式路线已经确定而自动删除。

## 仓库编辑约束

- 用户当前允许的是移动、重命名，以及已经确认的入口/索引/架构说明修正。
- **任何删除文件、删除目录、覆盖历史证据、合并后移除原文件等操作，都必须先取得用户明确同意。**
- GitHub API 的“新建目标文件 + 删除旧文件”本质上包含删除，不得把它当成无需确认的普通重命名。
- `docs/工作记录/` 保持原目录名和历史时间顺序；除非用户单独批准，不移动、不重命名、不删除其中历史文件，也不批量重写历史结论。
- 不为了目录整洁随意拆分 `scripts/`；当前约定是 `scripts/` 保存可执行入口、诊断和历史比较工具，核心复用逻辑放在 `src/attention_pipeline/`。
- 不凭推测创建新的“正式配置”“正式 runtime”“production model”路径。必须先核验真实已运行资产的位置和内容。

## 数据与输出边界

- 原始/正式实验数据视为只读输入。
- 全量正式分析输出与大体量中间产物应保存在仓库外部，不直接纳入 Git。
- 仓库保存代码、配置、必要模型、训练数据集定义、runtime、测试、文档以及有限且有复现价值的历史证据产物。
- RGB、跨模态等模块是否开启以当前入口文档和用户明确指令为准，不因 NIR 全量完成而自动扩展范围。

## 工作方式

- 修改前先核验真实文件和当前路径，不能根据旧文档猜测。
- 当前入口文档负责描述“现在是什么状态”；历史工作记录负责说明“当时为什么这样做”。两者不要混用。
- 对已确认的历史事实可以保留原措辞，但如果旧措辞出现在 README、总览、索引等当前入口并与现状冲突，应更新入口而不是改写历史记录。
- 如果某个正式运行资产的真实位置尚未核验，明确写“未核验”，不要自行补一个看似合理的路径。
