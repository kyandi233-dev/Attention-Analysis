# DEPRECATED STATIC SNAPSHOT — DO NOT USE AS CURRENT COMPLETION EVIDENCE
#
# This file used to mix mutable source/document hashes with frozen binary-asset
# hashes. That design becomes stale whenever the runtime is repaired and is not
# an adequate provenance contract for the current complete RITnet full-class
# workflow.
#
# Current authoritative identities are:
#   1. executed source code: exact Git commit + clean working tree;
#   2. config: content SHA256 recorded in the per-run manifest/resume identity;
#   3. RITnet ONNX: content SHA256 recorded in the per-run manifest/resume identity;
#   4. RITnet external data (.onnx.data): independent content SHA256 recorded in
#      the per-run manifest/resume identity;
#   5. source eyes.csv and source video: content identity recorded per run;
#   6. output CSV / label_index / chunk_manifest / store_manifest / summary /
#      manifest / qc_index: SHA256 recorded and re-verified by completion.json.
#
# The canonical user entry points perform those checks at runtime. Do not copy
# hashes from an older checkout into a new run. If a standalone static checksum
# export is required for archival delivery, generate it from the exact frozen
# Git commit and delivered binary files at packaging time.
