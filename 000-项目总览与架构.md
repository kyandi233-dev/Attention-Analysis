# 000｜项目总览与架构

> 08-16（Asia/Shanghai）｜NIR高严重度修复和历史复核已完成；PuReST仍是生产候选，但正式三种人脸ROI在双眼特写上均未通过硬门，生产冻结已停止。

## 2026-08-21 当前工作阶段

> 2026-08-21 17:30（Asia/Shanghai）｜项目已从预实验管线审计转入正式数据分析准备阶段。

北京已完成一百多名正式被试的数据采集，预计 2026-08-23 晚回珠海后开始正式分析。NIR 眼框检测器正在另一台 AMD RX 6750 GRE 12GB 电脑上使用 CPU 完成 100 epochs 训练；训练完成后，候选权重可转移至 NVIDIA 电脑进行批量推理。

当前正式分析架构为：

```text
OpenCV 读取视频
        ↓
是否需要重新检测？
   是：YOLO 检测并校正 ROI
   否：tracking 更新上一帧 ROI
        ↓
RITnet 处理眼睛 ROI
        ↓
连续性、可用性与失败状态 QC
        ↓
行为时间窗与 NIR 指标
```

这里的“是否需要重新检测”是管线中的控制逻辑，不是 OpenCV 自动完成的功能。重新检测条件、tracking 实现和 RITnet 参数必须先经过短视频基准测试；当前不视为已经冻结。

## 目录

- [项目边界](#项目边界)
- [文件地图](#文件地图)
- [审批门](#审批门)
- [命令速查](#命令速查)
- [当前决策](#当前决策)

## 项目边界

- 只读输入：`E:/预实验`、`E:/正式实验`、`020-Experiment/_AttentionTest`、旧 `NIR-RGB-extraction`。正式 NIR 约 30 cm 近桌面仰拍，补光灯与相机分离并向下照射，不能直接沿用预实验成像参数。
- 正式输出：`D:/_AttentionData/output-v2/040-pre-experiment`；行为阶段已按批准写入，未与v1混写。
- 主模块：`010-nir` 与 `040-behavior`。
- `020-rgb`、`030-cross-modal` 仅为关闭的保留接口。
- 第一阶段不输出 rPPG、跨模态融合、个体/群体专注分数或日常场景效度声明。

## 文件地图

| 编号 | 入口 | 用途 |
|---|---|---|
| 000 | 本文件 | 总览、边界、审批与决策导航 |
| 001 | `configs/preexperiment.yaml` | 路径、被试、窗口、阈值和阶段开关的唯一配置 |
| 010 | `docs/010-nir/00-目录与映射.md` | NIR 测量、标注、评价和报告 |
| 020 | `docs/020-rgb/00-目录与映射.md` | RGB 关闭接口 |
| 030 | `docs/030-cross-modal/00-目录与映射.md` | 跨模态关闭接口 |
| 040 | `docs/040-behavior/00-目录与映射.md` | 行为数据与证据窗 |
| — | [docs/工作记录/](docs/工作记录/00-目录与映射.md) | 工作记录唯一入口；含08-13/08-14历史证据及08-16正式管线、PuReST与修复审计 |

## 审批门

1. **骨架与小样本复核**：12帧/24眼审批包已完成并归档于`finish/gate1-24eyes/`。
2. **528眼真值、阶段4/4b与5**：人工真值已完成；轴角修复后六算法/18配置仍无准入者；PuReST保留为连续生产候选。
3. **正式ROI入围门**：sub-011 Block1 60时点已复核。MediaPipe、YuNet与当前YOLO-face正确双眼ROI均为0/60，故停止尺度调优、正式px冻结和生产打包。
4. **行为证据与专注定义**：041–045证据、图表、修改建议和字段字典已完成；专注评分仍未冻结。

只有新的ROI候选通过正确双眼身份、≥95%人工可用率和≤2%严重裁断率后，才恢复正式PuReST参数冻结与完整生产输出。
## 命令速查

```powershell
$env:PYTHONPATH='src'
& 'D:/Code/python/python.exe' -m pytest -q
& 'D:/Code/python/python.exe' scripts/gate1_contract_check.py

# 当前只允许ROI入围诊断；不是正式生产分析
& 'D:/Code/python/python.exe' scripts/roi_compare.py `
  --config configs/formal.yaml --subject sub-011_ --frames 9441,10341,11241,12141,13040
```

正式生产入口`scripts/run_nir_pipeline.py`尚未生成，因为没有ROI候选通过硬门。`rgb`、跨模态和专注评分仍无生产实现。
## NIR 当前状态与停止门

- 椭圆轴角已修复为原始表示与canonical表示双轨；轴交换同步旋转90°，mask与光度使用原始size/angle。
- 阶段4/4b已在`artifacts/benchmark-axis-fix-review/`独立复跑；最佳约17.2%，仍无算法或参数配置达到准入门槛。
- 阶段5已在`artifacts/sequence-adapter-review/`复跑；PuReST可见覆盖0.5230、恢复192 ms，连续性优于PuRe，但仍只是预实验代理标签证据。
- PuReST采用普通连续调用＋`minPupilDiameterMM=0.5`兼容设置＋Python严格px后置门；不能称为内部全程严格px控制。
- 正式Block1画面为双眼/鼻梁特写。MediaPipe与YuNet需要完整人脸；当前YOLO-face把单眼区域当脸。三者正确双眼ROI均0/60。
- 当前停止门：批准并验证适用于“双眼特写”的新ROI路线前，不运行尺度网格、不冻结正式px/门控、不复制生产模型、不生成单命令生产脚本。
## 当前决策

| 决策项 | 当前结果 | 依据 |
|---|---|---|
| 专注定义 | 暂不冻结 | 先检查行为证据、不确定性及四类探针敏感性 |
| RT | 全保留并加 QC 标记 | 正式程序已把按键归属到试次；阈值不是删除依据 |
| NIR 算法 | PuReST仍为瞳孔候选；生产链未冻结 | 历史修复复核通过，但三种正式ROI均失败；PuRe仅作抽样质控/诊断 |
| NIR 审批包 | 12帧/24眼已复核，归档于 `finish/gate1-24eyes/` | 011 第一审批门表；帧号/block 已核验 |
| 协议配置 | 历史`protocol`（ABCCBA/四类）与当前正式`formal_protocol`（BBB、18×24、注意4分类＋警觉度4点）拆分 | 正式程序08-15后已更新；行为历史分析仍依赖旧节 |
| 插值 | 算法准确率阶段禁用 | 缺失、观测和插值必须分轨 |
| RGB | 关闭 | rPPG 方法尚未验证，避免迁移未经证实结论 |
| 行为程序修改 | 仅形成14条分层建议，未实施 | 等待用户逐项批准；正式程序保持只读 |
| 行为证据包 | 041–045完成 | 17张图、逐试次/block/窗口/探针对照、建议表与80字段字典 |
| 模型迁移 | 暂停；新ROI定稿后再建v2 `models/`与`runtime/` | 生产链全部实际模型均复制，ROI候选只复制最终入选者；当前三候选均淘汰 |



