# 000｜项目总览与架构

## 2026-08-23 当前工作阶段

正式 NIR 全量分析已经完成。项目当前不处于“准备正式分析”“等待 ROI 准入”或“尚未生成正式生产链”的阶段；现阶段主要任务是整理仓库、区分正式资产与历史候选、补齐复现入口，并保证已有研究过程和历史证据可追溯。

已完成的关键节点：

- 一百多名正式被试的数据采集完成。
- NIR 双眼 ROI 数据集 v1 完成。
- YOLO26n 眼框检测器完成 100 epochs 训练与冻结 test 评价。
- 冻结 test：7 名被试、85 张图、169 个眼框；mAP50 = 0.9913，mAP50-95 = 0.6589。
- YOLO `best.pt` 已进入实际后续处理流程，不是未来待训练资产。
- YOLO + tracking + RITnet 的正式 NIR 分析流程已经对正式数据完成全量运行。

## 当前 NIR 架构

```text
NIR video
    ↓
OpenCV 逐帧读取
    ↓
YOLO 周期性重新检测双眼 bbox
    ↓
tracking / 上一帧 ROI 更新中间帧
    ↓
按 bbox 裁剪双眼 ROI
    ↓
RITnet 瞳孔 / 虹膜分割
    ↓
连续性、可用性与失败状态 QC
    ↓
行为时间窗对齐与 NIR 指标输出
```

这里的 tracking 用来降低每帧都运行 YOLO 的计算量；YOLO 负责重新校正 ROI，RITnet 在裁剪后的眼睛 ROI 上执行像素级分割。当前仓库整理的重点是把这条已经运行过的正式路线与更早的 full-face ROI、PuRe/PuReST、DeepVOG、YuNet、MediaPipe 等候选/比较路线区分清楚。

## 代码与运行资产的实际分层

当前仓库不是“一个 CLI 对应全部历史和当前逻辑”的单体结构，而是经过研究迭代形成的三层：

```text
src/attention_pipeline/
    可复用核心、行为分析、历史 NIR review / benchmark / sequence

scripts/
    数据集构建、模型评价、历史 ROI / pupil 比较、诊断和一次性入口

runtime/nir-yolo-tracking-ritnet-v1/
    2026-08-22 冻结的 portable YOLO26n + tracking + RITnet 完整实现
```

当前已核验的 YOLO + tracking + RITnet 端到端实现入口是：

```text
runtime/nir-yolo-tracking-ritnet-v1/run_pipeline.py
```

它支持 YOLO、CSRT/KCF、周期重检测、ROI 扩展、RITnet、逐帧/逐眼状态、timestamp、运行 manifest 与 `--full-video`。runtime 中的 YOLO 权重与正式训练 `best.pt` 为同一 Git blob；RITnet 权重与 `models/RITnet-master/best_model.pkl` 也为同一 Git blob。

这并不等于 package 内默认参数已经被证明是全量最终参数。该包建立时仍属于跨机准入 package；当前分支没有保留正式 full-run 的最终 `run_manifest.json` 或全量输出目录，也没有 2026-08-22 之后的 committed 工作记录。因此最终 tracker、重检测频率和 ROI expansion 仍属于 provenance 待闭合项。

## 项目边界

- 正式数据与大体量输出不直接纳入 Git 仓库；仓库保存代码、配置、模型、数据集定义、必要 runtime、测试、文档与有限的历史证据产物。
- `010-nir` 是当前 NIR 核心；`020-rgb`、`030-cross-modal` 继续作为保留接口，不因 NIR 全量完成而自动开启。
- 行为分析与 NIR 指标可以共享时间窗/协议定义，但不把不同阶段的历史配置强行合并成一套。
- 历史工作记录用于 provenance；当前入口文档负责描述当前真实状态。两者用途不同，历史文档不因路线变化而批量重写。
- 全量输出本体可以继续留在数据盘，但轻量 `run_manifest.json`、最终参数摘要或对应工作记录应在能够找到原始证据后补回仓库，以闭合复现链。

## 文件地图

| 编号 / 目录 | 入口 | 用途 |
|---|---|---|
| 000 | 本文件 | 当前项目状态、边界与架构总览 |
| docs | `docs/00-目录与映射.md` | 文档总目录 |
| 010 | `docs/010-nir/00-目录与映射.md` | NIR 方法、评价、历史方案与正式分析文档 |
| 020 | `docs/020-rgb/00-目录与映射.md` | RGB 保留接口 |
| 030 | `docs/030-cross-modal/00-目录与映射.md` | 跨模态保留接口 |
| 040 | `docs/040-behavior/00-目录与映射.md` | 行为数据、时间窗与证据 |
| 050 | `docs/050-sart-formal/00-目录与映射.md` | 正式 SART 行为分析 |
| 060 | `docs/060-methods/` | 跨模块方法说明 |
| 070 | `docs/070-architecture/` | pipeline 架构与数据流 |
| 080 | `docs/080-decisions/` | 技术选择与决策记录 |
| — | `src/attention_pipeline/00-目录与映射.md` | 核心源码演化与模块边界 |
| — | `scripts/00-目录与映射.md` | 脚本职责与入口索引 |
| — | `runtime/README.md` | runtime / frozen portable package 角色说明 |
| — | `docs/工作记录/00-目录与映射.md` | 历史计划、执行证据和研究过程唯一入口 |

## 正式路线与历史路线的边界

### 当前正式路线

当前正式 NIR 主链以训练后的 YOLO 眼框模型、周期性重新检测、tracking 和 RITnet 为核心。全量分析已经完成，因此仓库中的任何“候选”“等待准入”“正式生产尚未开始”等描述，如果出现在当前入口文档中，都应视为需要更新的历史残留。

### 历史候选与比较路线

以下内容继续保留，因为它们构成方法选择和研究过程的证据：

- MediaPipe FaceLandmarker ROI
- YuNet ROI
- YOLO-face / face-parts 候选
- PuRe / PuReST 与六算法基准
- DeepVOG / pye3d / Iris 等比较或外部方案
- Gate1、阶段 4/4b/5、ROI selection、smoke test 等历史 artifacts

这些资产不等于当前正式路线，也不因当前路线已经确定而删除。

## 配置说明

`configs/formal.yaml` 是早期 ROI 入围阶段留下的历史兼容配置。它内部仍记录 `0.1.0-candidate`、full-face detector 不适用、production 未冻结等旧状态，因此不能再作为“当前项目是否完成正式分析”的判断依据。

`runtime/nir-yolo-tracking-ritnet-v1/config.yaml` 是 later portable route 的默认准入配置。它记录的 YOLO threshold、tracker、重检测间隔和 ROI expansion 可以解释 package 建立时的运行默认值，但在没有最终 full-run manifest 时不能直接提升为“全量最终冻结配置”。

整理时遵循：

1. 现行入口文档反映当前真实阶段。
2. 历史配置保留用于复现，不强行伪装成当前配置。
3. frozen package 本身不为适配当前文字而直接改写，避免破坏 ZIP / SHA256 一致性。
4. 如果找到正式全量运行的原始 manifest 或参数摘要，再建立清晰的 final-run provenance 入口。

## 当前整理原则

- 不删除历史文件；任何删除先单独确认。
- 不批量改写 `docs/工作记录/` 的历史正文。
- 可以更新当前入口、索引、架构说明和明显错误的路径映射。
- `src/attention_pipeline/` 保存可复用核心逻辑；`scripts/` 保持命令入口、诊断和历史比较工具的角色，不再复制核心实现。
- 数据集、权重、第三方模型、runtime 包和 artifacts 按“当前正式资产 / 历史复现资产 / 外部依赖”区分职责，而不是按是否最新直接删除。
