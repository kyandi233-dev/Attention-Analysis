# Legacy behavior package

`src/attention_pipeline/behavior/` 保存早期通用 Behavior 分析实现，当前正式 FocusWave v3.1.3 BB 分析已经迁移到 `src/attention_pipeline/behavior_formal/`，历史 BBB v3.0 则独立保存在 `src/attention_pipeline/behavior_bbb_v3_0/`。

本目录当前不作为正式入口。其源码暂时保留用于历史兼容与依赖审计；是否最终删除需按仓库删除规则另行授权。
